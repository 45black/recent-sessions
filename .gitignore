# Instance-local state — generated by setup.sh
config.env

# macOS junk
.DS_Store

# Safety net: never commit anything that looks like a secret cache
secrets/
*.secret

# Session-checkpoint handover artefacts — these land in $PWD and are
# per-session, not part of the skill source tree.
HANDOVER-*.md

